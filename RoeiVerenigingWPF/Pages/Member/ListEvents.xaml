<Page x:Class="RoeiVerenigingWPF.Pages.Member.ListEvents"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:RoeiVerenigingWPF.helpers"
      mc:Ignorable="d" 
      Height="900" Width="1000"
      Title="ListEvents">

    <Grid>
        <Border CornerRadius="15" BorderThickness="5" BorderBrush="{x:Static local:CustomColors.OutsideBorderColor}">
            <Border CornerRadius="10" BorderThickness="8"
        BorderBrush="{x:Static local:CustomColors.MainBackgroundColor}">
                <Grid Height="900" Width="1000" Background="{x:Static local:CustomColors.MainBackgroundColor}"
          HorizontalAlignment="Center"
          VerticalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>

                    <Label Grid.Row="0"
                Content="Evenements" HorizontalContentAlignment="Center" VerticalAlignment="Top"
                Margin="0,30,0,0"
                FontFamily="Franklin Gothic Medium" FontSize="36"
                Foreground="{x:Static local:CustomColors.HeaderColor}" Grid.ColumnSpan="2" />

                    <Button Grid.Row="0" Grid.Column="1" Width="230" Height="70" HorizontalAlignment="Right" VerticalAlignment="Top"
                    Margin="0,20,20,0"
                    Content="Evenement aanmaken"
                    FontFamily="Franklin Gothic Medium" FontSize="20"
                    Foreground="{x:Static local:CustomColors.HeaderColor}"
                    Background="{x:Static local:CustomColors.ButtonBackgroundColor}"
                    BorderBrush="{x:Static local:CustomColors.OutsideBorderColor}">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="15" />
                            </Style>
                        </Button.Resources>
                    </Button>
                    <Grid Grid.Row="1">
                        <ListView ItemsSource="{Binding boats}" Grid.IsSharedSizeScope="True"
                         d:ItemsSource="{d:SampleData ItemCount=5}" Name="ListView"
                         ScrollViewer.VerticalScrollBarVisibility="Auto" BorderBrush="Transparent" MaxHeight="750">
                            <!--Loaded="ListView_Loaded voor in list view ophalen-->
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListViewItem">
                                                <Border Background="{TemplateBinding Background}">
                                                    <ContentPresenter />
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>

                            </ListView.ItemContainerStyle>
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="1"/>
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <!--Grids that get repicated-->
                                    <Grid Height="300" Width="900" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0, 10, 0, 10">
                                        <Grid.Effect>
                                            <DropShadowEffect Color="Black" Direction="320" ShadowDepth="10" Opacity="0.3" />
                                        </Grid.Effect>
                                        <Border CornerRadius="30" BorderThickness="5" BorderBrush="{x:Static local:CustomColors.OutsideBorderColor}">
                                            <Border CornerRadius="24" BorderThickness="8"
                                                BorderBrush="{x:Static local:CustomColors.MainBackgroundColor}"
                                                Background="{x:Static local:CustomColors.MainBackgroundColor}">

                                                <Grid Height="400" Width="900">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="1*" />
                                                        <ColumnDefinition Width="1*" />
                                                    </Grid.ColumnDefinitions>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="80"/>
                                                        <RowDefinition/>
                                                    </Grid.RowDefinitions>
                                                    <!--left-->
                                                    <StackPanel Grid.Row="0" Grid.Column="0">
                                                        <TextBlock Text="Naam" Foreground="{x:Static local:CustomColors.HeaderColor}" FontSize="32"></TextBlock>
                                                    </StackPanel>
                                                    <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2">
                                                        <TextBlock Text="Beschrijving:" Foreground="{x:Static local:CustomColors.HeaderColor}" FontSize="20" TextWrapping="Wrap"></TextBlock>
                                                    </StackPanel>

                                                    <!--right-->
                                                    <StackPanel Grid.Row="0" Grid.Column="1">
                                                        <TextBlock Text="begin Datum" HorizontalAlignment="Right" Margin="0, 0, 30 ,0" FontSize="20"></TextBlock>
                                                        <TextBlock Text="eind Datum" HorizontalAlignment="Right" Margin="0, 0, 30 ,0" FontSize="20"></TextBlock>
                                                        <TextBlock Text="Aantal mensen" HorizontalAlignment="Right" Margin="0, 0, 30 ,0" FontSize="20"></TextBlock>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </Border>
                                    </Grid>

                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Grid>
            </Border>
        </Border>
    </Grid>
</Page>
